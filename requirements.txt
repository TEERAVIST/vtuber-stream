numpy>=1.26,<2
scipy>=1.12
soundfile>=0.12
transformers>=4.44
accelerate>=0.33
peft>=0.11
sentencepiece>=0.1.99
PyYAML>=6.0
pypdf>=4.2
nltk>=3.8
faiss-cpu>=1.7.4,<2
safetensors>=0.4.3
einops>=0.7.0

# Note: Torch is provided by the RunPod PyTorch image. We intentionally do not
# pin/install torch here to avoid huge downloads and CUDA mismatches.
# If running outside that image, install torch that matches your CUDA.
